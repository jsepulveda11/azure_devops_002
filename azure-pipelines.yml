# azure-pipelines.yml
trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: 000-S3-AD

stages:
- stage: PythonConfig
  jobs:
  - job: PythonConfigJob
    steps:
    - template: /templates/template-python.yml

- stage: NodeJsConfig
  jobs:
  - job: NodeJsConfigJob
    steps:
    - template: /templates/template-nodeJS.yml